# Batch resize photos to 'retina' size

## Option
### path (default cwd)
### --backup (default False)
Save original files.\
### --format (jpeg or webp, default jpeg)
For webp pillow must be complied with WebP support (first install libwebp-dev)
